/**
 * Route constants — single source of truth for app route paths.
 *
 * Re-exports from `routes/routes.ts` for convenient access
 * throughout the application.
 */
export { ROUTES } from '../routes/routes';
