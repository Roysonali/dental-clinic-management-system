/**
 * DensCare Border Radius Tokens
 *
 * Values match the `@theme` declarations in `index.css`.
 */

export const radius = {
  sm: '0.25rem',
  md: '0.375rem',
  lg: '0.5rem',
  xl: '0.75rem',
} as const;
